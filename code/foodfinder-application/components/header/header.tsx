import Logo from "components/header/logo"

const Header = function (): JSX.Element {
    return (
        <header>
            <div>
                <Logo />
            </div>
        </header>
    )
}

export default Header
